# UDR report QA - required changes

Status: required before the next client-facing UDR report revision.

## Pages 6-8: explain the changing business sets

- Retitle page 6 to make clear that it is the leading recommendation market, including unresolved names.
- State that the page 6 chart is limited to the highest-frequency results and is not the detailed comparison cohort.
- Add Silver Star to page 6 if the layout remains readable. If it is not shown, explicitly state why a lower-frequency business can still be selected for deeper comparison.
- Label pages 7-8 consistently as the selected local comparison set.
- Explain the selection criteria: verified identity, geographic relevance and sufficient website/review evidence.

## Page 9: use consistent comparable values

- Replace every instance of `Several` in `Questions they appeared for` with an exact `x / 8` value:
  - UDR Properties: `3 / 8`
  - Why Bother Cleaning: `3 / 8`
  - Silver Star Cleaning: `2 / 8`
  - Stellar End of Tenancy Cleaning: `3 / 8`
- Replace `Available` in `Website pages reviewed` with exact page counts:
  - UDR Properties: `20 pages`
  - Why Bother Cleaning: `20 pages`
  - Silver Star Cleaning: `10 pages`
  - Stellar End of Tenancy Cleaning: `20 pages`
- Keep one format and unit across every cell in a comparison row.

## Page 10: resolve the unbalanced three-card layout

- Retain a 2x2 grid and add a fourth evidence-backed strength: `Easy ways for customers to take the next step`.
- Support it with the existing website evidence for pricing, FAQs, booking routes and contact options.
- Tighten the existing `Strong website basics` card so this evidence is not duplicated.
- If the fourth strength cannot be supported cleanly in a future report, switch the section to three full-width stacked rows instead of leaving one empty grid position.

## Reusable report rule

- Comparison tables must use a consistent type of value within each row. Do not mix numbers with qualitative placeholders such as `Several` or `Available` when an exact measured value exists.
- Whenever adjacent pages use different business sets, label both sets and explain the inclusion criteria on the page where the change occurs.
